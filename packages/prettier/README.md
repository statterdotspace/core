# @statter/prettier

Shared Prettier config for consistent formatting across projects.

## Install

```bash
yarn add -D @statter/prettier prettier
```

## Usage

Create `prettier.config.mjs`:

```js
import { prettier } from "@statter/prettier"

export default prettier
```

## Build (for maintainers)

```bash
yarn build
```
